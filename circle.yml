# machine:
#   services:
#     - docker
dependencies:
  override:
    - ssh -i ~/.ssh/config ubuntu@54.169.218.108 bash -c "chef gem install kitchen-docker;pushd /home/ubuntu/chef-repo/cookbooks/dirTest;kitchen test"
