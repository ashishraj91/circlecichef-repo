# machine:
#   services:
#     - docker
dependencies:
  override:
    - ssh -i ~/.ssh/config ubuntu@54.169.218.108 bash -c "cp ~/${circlecichef-repo} /home/ubuntu/chef-repo/cookbook;pushd /home/ubuntu/chef-repo/cookbook/${CIRCLE_PROJECT_REPONAME};chef exec kitchen test"
    