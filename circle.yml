# machine:
#   services:
#     - docker
dependencies:
  override:
    - ssh -i ~/.ssh/config ubuntu@54.169.218.108 bash -c "echo $CIRCLE_PROJECT_REPONAME"
    - ssh -i ~/.ssh/config ubuntu@54.169.218.108 bash -c "echo $CIRCLE_PROJECT_REPONAME;cp ~/$CIRCLE_PROJECT_REPONAME /home/ubuntu/chef-repo/cookbook;pushd /home/ubuntu/chef-repo/cookbook/${CIRCLE_PROJECT_REPONAME};chef exec kitchen test"
    