# machine:
#   services:
#     - docker
dependencies:
  override:
    - scp -i ~/.ssh/config -rp /home/ubuntu/${CIRCLE_PROJECT_REPONAME} ubuntu@54.169.218.108:cookbooks/dirTest
    - ssh -i ~/.ssh/config ubuntu@54.169.218.108 bash -c "ls -l;pushd /home/ubuntu/cookbooks/dirTest;sudo kitchen test;sudo berks install"
