# machine:
#   services:
#     - docker
dependencies:
  override:
    - ssh -i ~/.ssh/config ubuntu@54.169.218.108 bash -c "echo ${CIRCLE_PROJECT_REPONAME};cp -avip ~/${CIRCLE_PROJECT_REPONAME} /home/ubuntu/chef-repo/cookbooks;pushd /home/ubuntu/chef-repo/cookbook/${CIRCLE_PROJECT_REPONAME};kitchen test"
